{
  "rules": {
    "rooms": {
      "$uid": {
        ".read": true,
        ".write": "auth.uid === $uid"
      }
    },
    "pairing": {
      "$pairingId": {
        ".read": true,
        ".write": "auth != null"
      }
    }
  }
}
